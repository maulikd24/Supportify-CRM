# Marketing site (supportify.co.in) lives in website/ — static HTML, no build step.
[build]
  base = "website"
  publish = "."
  command = ""
  # Skip Netlify builds when a commit doesn't touch website/.
  ignore = "git diff --quiet $CACHED_COMMIT_REF $COMMIT_REF -- ."
